---
name: recheck-2026-07-21
description: สถานะหลังกวาดทั้งระบบ 21 ก.ค. 2026 — มิเกรชัน 165-171 รันครบแล้ว + พื้นที่ที่ยังไม่ได้ audit
metadata: 
  node_type: memory
  type: project
  originSessionId: b8d73488-693d-46d1-b5f3-7849cf1f6166
  modified: 2026-07-21T12:15:51.667Z
---

**21 ก.ค. 2026** กวาดทั้งระบบ (full-system recheck) หลังแก้รีวิว 64 ข้อจบ — ใช้ workflow fan-out + adversarial verify

**แก้แล้ว push แล้ว (v495–v497 + api/migration):** 4 regression ของวันนั้นเอง (web-order throttle stamp ก่อน insert, DocCapture ตกหมายเหตุลูกค้า, MaterialPrep จากใบงานคิดหน่วยสินค้า 2 หน่วยผิด 100 เท่า, created_by upsert ทับใน quotation/BOQ) + 13 บั๊กเก่า (ลบบัญชีทำ profile กำพร้า, mentor token ไม่เช็ก role สด, OT hours, PayDetail บรรทัดภาษี, โควตาลารายคน, dashboard ยอดค้างจ่าย, ป้ายช่าง, listWebOrders 1000-cap, team-chat unread 500-window, jobDate UTC, AI bot cap นับ .length ตัน 1000, graphic RLS)

**มิเกรชันที่รันครบแล้ว (เจ้าของยืนยัน):** 165 (นาฬิกาเข้างานยึดเซิร์ฟเวอร์) · 167 (ปิดสิทธิ์ช่างอ่านราคา — allowlist admin/exec/finance/sales/stock/hr) · 168 (pg_trgm ค้นแชต) · 169 (web_order rate limit) · 171 (graphic web read) · 170_tm_chat (แชต mentor, ถ้ารัน) · live db ถึง 171 แล้ว
   - ⚠️ 165 numbered ต่ำกว่า 166 แต่รันทีหลัง (มิเกรชันโปรเจกต์นี้รันข้ามลำดับได้) · เคยตั้งเลข 170 ชนกับ 170_tm_chat → ของ recheck เป็น 171

**❌ แอป Mentor = คนละโปรเจกต์ เจ้าของสั่งชัด "ไม่ต้องทำอะไร"** — ห้ามแก้/กวาด/แตะ the-top-mentor/, app/public/mentor/, app/api/mentor-*.js, ตาราง tm_* แม้ critic จะเจอ lost-update race ที่ tm_members.data ก็ตาม (ยกเว้นเจ้าของสั่งเอง)

**ยังไม่ได้ audit ในฝั่ง ERP (งานถัดไปถ้าจะกวาดต่อ):**
- โค้ดพิมพ์ printDoc.js / DocSlip discountCol — ไม่มีใครเปิด
- serverless: flowaccount-doc.js (side-effect ภายนอก), hr-cron.js (promise ไม่ await บน Vercel), fb-send.js
- realtime RLS: TeamChat subscribe ทุกห้องพึ่ง RLS กรอง — ต้องเช็กกับ publication config จริง (นอก repo)

ดู [[build-passes-page-dead]] — บทเรียนหลัก: build/เทสต์เขียว ≠ หน้าเปิดได้ เพราะไม่มีเทสต์เปิดหน้าจริง เจ้าของเจอ 2 หน้าตายด้วยการกดเมนู
