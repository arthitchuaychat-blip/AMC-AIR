---
name: hr-system
description: "HR module — attendance check-in/out, leave, holidays, stats; schedules + setup"
metadata: 
  node_type: memory
  type: project
  originSessionId: 85b6d010-27bd-419a-ad75-1ac380626a9a
---

HR system (built 2026-06-16, v63). Two pages + permission modules `attendance` (everyone, self-service) and `hr` (admin/exec).

- **Attendance.jsx** (เข้างาน/ลา): selfie (`<input capture=user>`) + GPS check-in/out → `hr_attendance`; auto late/OT from `lib/hr.js dayStat()`. Leave requests (sick/personal/vacation) sized by working days `leaveDays()`; remaining balance = quota − approved this year. **Gotcha (v221):** the check-in button is `fileRef.click()` on a `capture="user"` file input — **LINE/Facebook in-app browsers and bare Android WebViews block the camera**, so the button "does nothing" (no error). `inAppBrowser()` (UA sniff: `Line/`, `FBAN|FBAV|FB_IAB`, Instagram/Messenger/WeChat/TikTok, `; wv)`) shows an orange warning + copy-link telling them to open in Chrome/Safari or add-to-home-screen. So "X can't check in" but others can = X opened the app inside an in-app browser, not a code/account bug.
- **HR.jsx** (admin): today dashboard (in/late/absent/leave), leave approval (`decideLeave`), monthly report (worst-first ranking + CSV export), per-staff กะ (work_pattern + sat_group), holidays, settings (work hours/grace/quota).
- **lib/hr.js** (pure, Node-tested): `isWorkday(date,pattern,satGroup,holidaySet)`, `dayStat`, `leaveDays`. Patterns: `mon_sat` / `mon_fri` / `mon_fri_alt_sat` (เสาร์เว้นเสาร์ via week-parity: A=even weeks, B=odd) / `none`. Default hours 08:00–17:00, grace 15m, quota พักร้อน6/ลากิจ3/ลาป่วย30 (Thai law defaults, editable).

Data: migration `041_hr.sql` (hr_attendance, hr_leaves, hr_holidays, hr_leave_quota + profiles add hire_date/department/work_pattern/sat_group; RLS self + admin/exec). hr_settings stored in `app_config` (needs migration 039 from [[permissions-system]]). Selfies → existing `photos` storage bucket (`uploadAttendancePhoto`).

**Payroll + Performance (v98–v99, 2026-06-20 — needs migration `051_payroll.sql`).** Pay cycle: **cut-off 25th** (period = 26th prev → 25th this month via `payPeriod(ym)` in `lib/payroll.js`), paid month-end. Per-employee pay settings on `profiles` (mig 051): `pay_type` monthly|daily, `base_pay`, `ot_rate` (฿/OT-hr), `sso` bool — edited in HR → กะ&ตั้งค่า ("ค่าจ้าง/เงินเดือน" card). HR tab **"เงินเดือน"** (`PayrollTab`): `periodStats()` + `computePayslip()` (pure, lib/payroll.js) → base (monthly=full / daily=present×wage) + OT(hrs×rate) − late(lateMin→hr×hourly) − absence(monthly only) − over-quota-leave(annual used−quota, monthly) − SSO 5% cap 750; editable bonus/other-deduct; saves to `payslips` table (one per period+user, status draft|paid), "บันทึกรอบ"/"ทำจ่ายทั้งรอบ". api: listPayslips/savePayslip/setPayslipPaid. HR tab **"ประสิทธิผล"** (`PerfTab`): per-employee scorecard = attendance (on-time%/absent/late/OT from periodStats) + their **team's** jobs (done/avg rating/claims/reschedule — jobs link to team, not person) → composite score (on-time 50% + present 20% + rating 30% − claims×5). Both gated to admin/exec via HR module; payslips RLS admin/exec/finance.

**Cash advances / เบิกเงินล่วงหน้า (v125, 2026-06-21 — needs migration `053_advances.sql`).** Staff request from **Attendance.jsx** ("เบิกเงินล่วงหน้า" card: amount+reason → `submitAdvance`); see own list + cancel while pending (`listMyAdvances`/`cancelMyAdvance`). HR tab **"เบิกล่วงหน้า"** (`AdvancesTab`) approve/reject (`listAdvances`/`decideAdvance`). Status flow pending→approved→paid (+rejected). Approved-and-unsettled (period null) advances are auto-summed per user in `PayrollTab` and deducted as `dAdvance` (computePayslip in lib/payroll.js; payslips.d_advance col; new "หักเบิกล่วงหน้า" table column + slip line); on "ทำจ่ายทั้งรอบ" they're settled via `markAdvancesPaid(ym, ids)` (status=paid, period=ym) so they never double-deduct. Table `hr_advances` RLS = self OR admin/exec/finance. NOT wired into cash flow ledger.

**Setup the user must do:** run migration 041 (and 039 if not yet) + `053_advances.sql` in Supabase, then in บุคคล(HR) → กะ&ตั้งค่า set each staff's work_pattern (sales 2 คน = mon_fri_alt_sat, groups A/B).

**Unpaid + hourly leave (v407, 2026-07-16 — needs migration `141_leave_unpaid_hourly.sql`).** Leave type `unpaid` (ลาไม่รับค่าแรง): no quota (balance cards skip it), ALWAYS deducted for monthly staff via the over-quota channel — in PayrollTab `st.overLeave = min(leaveDays − unpaidLeave, over) + unpaidLeave` (daily-wage staff naturally unpaid via absence math). Hourly leave (ลาราย ชม.): rows carry `hours/time_from/time_to`, days stored as fraction (`LEAVE_HOURS_PER_DAY = 8`, half-hour steps); `buildLeaveDaySet(leaves, from, to)` in lib/hr.js returns `{t, h}` per user-day and is the SINGLE builder used by today/report/perf/payroll — an hourly-leave day still counts attendance (present/late/absent) and shows a blue "ลา x ชม." tag; `leaveFrac`/`leaveAmountText` helpers. submitLeave/updateLeave pass hours through with pre-141 fallback (strip columns on error).

**HR hardening batch (v423, 2026-07-17 — needs migration `144_ot_approval.sql`).** From the HR review:
- **1000-row cap fixed**: `listAttendance`/`listLeaves`/`listAdvances` now use `_fetchAll` (leaves/advances have no date filter and grow forever — past 1000 rows old ones silently vanished from reports/payroll; see [[supabase-1000-row-cap]]).
- **OT approval mode** (`settings.otNeedsApproval`, toggle in กะ&ตั้งค่า — the flag existed since day 1 but nothing used it): when ON, OT counts toward payroll/report **only** on days HR pressed "รับรอง OT" (TodayTab button → `setAttendanceOtOk` → `hr_attendance.ot_ok`, mig 144). Gating lives in `periodStats` (payroll.js) + ReportTab's inline loop; tags show "· รอรับรอง"/"✓". OFF (default) = old behavior, safe to deploy before running the migration.
- **TodayTab shows the evidence**: check-in/out selfie thumbnails (click = full) + 📍 GPS link per row (columns existed since 041, never displayed).
- **Forgot-checkout banner**: TodayTab amber banner lists last-7-days rows with check_in but no check_out; click opens AttEditModal for THAT day (edit state now `{p, a, day}` — modal uses `edit.day || day`).
- QuotaCard inputs save onBlur (was an API call per keystroke).

**HR hardening round 2 (v425, 2026-07-17 — needs migration `145_hr_rls_tighten.sql`).**
- **RLS hole closed**: old policies (082) were `for all + user_id = auth.uid()` → any employee could REST-PATCH their own leave to approved / rewrite their own attendance times / edit own advances (anon key is public). Mig 145: self = SELECT + INSERT-pending (leaves/advances) + INSERT/UPDATE own attendance for Bangkok-today only; a `hr_att_guard` trigger additionally blocks changing already-set times, forces new times ≈ now() (±10 min), and strips self-set `ot_ok`. Managers (admin/exec/hr; +finance for advances) keep full access.
- **UI self-approval lock**: LeavesTab/AdvancesTab now take `lockSelfId` (= selfId when role hr) — hr role sees 🔒 "ของตัวเอง" instead of approve/edit/delete on their own rows (same pattern as attendance lockSelfId; admin/exec still can).
- **PayrollTab pending-OT banner**: when otNeedsApproval is on, load() collects the period's unapproved-OT days into `pendingOt` → amber banner with per-day approve chips + "รับรองทั้งหมด" (so nobody silently loses OT pay).
- **Cross-year leaves**: `leaveDaysInYear(l, year)` in lib/hr.js prorates a Dec–Jan leave by calendar days per year; used in PayrollTab yearUsed, QuotaCard, Attendance balance (was: whole leave counted into start year).
- **Holiday work surfaced**: periodStats also returns `holidayDays/holidayHours` (attendance on non-workdays — previously skipped invisibly, daily-wage staff earned nothing); payroll row shows 🔶 "ทำงานวันหยุด" hint — compensation stays MANUAL via the bonus column (auto 3× pay = future decision).
- cancelPay now requires a reason (confirmDialog prompt.required) + `logAudit(action:cancel_pay)`; saveRun writes all slips in ONE upsert (`savePayslips`) instead of a per-person loop; Attendance.jsx shows the employee their own forgot-checkout days (they can't self-fix under mig 145 — must tell HR).

**PAYROLL GO-LIVE (owner decision 2026-07-17):** first real payroll round = **2026-07 (26 มิ.ย. – 25 ก.ค. 2569)**, paid end of July — matches payPeriod() exactly, no code change. Pre-26/6 attendance/leave data must not affect pay (and doesn't: periodStats is window-scoped; over-quota deducts only the in-round delta since v426). ONE manual check before the first "ทำจ่ายทั้งรอบ": clear stale approved-unsettled advances (they deduct regardless of request date).

**HR round 3 (v426, 2026-07-17 — needs migration `146_leave_self_cancel.sql`).**
- **Over-quota deduction fixed (money bug)**: old formula deducted the ANNUAL cumulative overage every month that had any leave (and leave of a still-within-quota type got hit because another type was over). Now per type: `max(0, usedThru(periodEnd)−quota) − max(0, usedThru(dayBefore(periodStart))−quota)` — each over-quota day deducted exactly once, in the month it occurs. `usedThru` uses `leaveDaysInRange(l, yearStart, cutoff)` (new lib/hr.js helper; `leaveDaysInYear` is now a thin wrapper over it).
- Notifications for check-in / leave / advance now include role `hr` (were admin/exec[/finance] only).
- Employees can cancel their own PENDING leave (mig 146 self-delete policy + `cancelMyLeave` + ยกเลิก button in ใบลาของฉัน — mirrors advances).
- Report/Payroll/Perf effects depend on `[ym, settings]` (stats no longer computed with default settings if the tab mounts before hr_settings arrives).
- ReportTab: "วันหยุดที่มา" column + CSV cols (matches periodStats holidayDays/holidayHours).
- **Geofence (advisory)**: hr_settings `geo` ("lat, lng" string) + `geoKm` (radius, default 1) in กะ&ตั้งค่า; TodayTab shows orange "ไกลร้าน X กม." tag next to the GPS pin via `distKm` (Haversine, lib/hr.js). Display-only — never blocks check-in (customer-site work is normal).
- Attendance balance cards show "รออนุมัติ X" per leave type (pending leaves reserved-days hint).

**ใบขอ OT + หักเงินยืม/ค่าน้ำ/ค่าไฟ (v526, 2026-07-31 — needs mig `184_hr_ot_loan.sql`).** เจ้าของเคาะ: OT = คิดจาก**ใบขอที่อนุมัติเท่านั้น** (เลิกคิด OT อัตโนมัติจากเช็คเอาท์), ฟอร์มกรอกเวลาเริ่ม-เลิก, ค่าน้ำ/ค่าไฟ**กรอกใหม่ทุกเดือน**, เงินยืม**ผ่อนอัตโนมัติจนครบ**.
- **DB (mig 184):** `hr_ot` (ใบขอ OT: user_id/ot_date/time_from/time_to/hours/status pending→approved→paid/period · RLS self insert-pending + managers admin/exec/hr) · `hr_loans` (principal/installment/balance/status · HR เปิดยืม) · `hr_loan_payments` (unique loan+period กันหักซ้ำ) · `payslips` เพิ่ม `d_loan/d_water/d_electric`.
- **api.js:** OT = submitOt(กรอกเวลา→`otHoursFromTimes` บล็อก½ชม.)/listMyOt/cancelMyOt/listOt/decideOt/markOtPaid/unsettleOt · Loan = listLoans/listMyLoans/saveLoan(balance=principal)/deleteLoan/markLoanPaid(idempotent+recompute balance)/unsettleLoan · savePayslip/s เพิ่มคอลัมน์ + pre-184 fallback.
- **payroll.js:** computePayslip รับ emp.loan/water/electric → dLoan/dWater/dElectric เข้า dedBefore (ลด advCap) · OT ใช้ `st.otHours` ที่ผู้เรียก override · frozenPayslip อ่าน+รวม 3 คอลัมน์.
- **Attendance.jsx:** การ์ด "ขอทำ OT" (OtForm เวลาเริ่ม-เลิก + preview ชม.) + รายการ OT ของฉัน + "เงินยืมของฉัน" (read-only) · openMyPay override `stp.otHours` = OT อนุมัติในรอบ.
- **HR.jsx:** แท็บ **อนุมัติ OT** (OtTab mirror AdvancesTab) + **เงินยืม** (LoansTab: เปิด/แก้/ลบ, เห็นคงเหลือ) · **PayrollTab**: load listOt("approved")+listLoans(true) → otByUser (override st.otHours) + loanByUser (min installment,balance) · adj เพิ่ม water/electric (กรอกต่อรอบ, อ่านจาก slip.d_water/d_electric) · ตารางเพิ่มคอลัมน์ เงินยืม(auto)/ค่าน้ำ/ค่าไฟ · save d_loan/d_water/d_electric · "ทำจ่ายทั้งรอบ" → markOtPaid+markLoanPaid (payable.flatMap) · cancelPay → unsettleOt+unsettleLoan · **ปิด pendingOt banner (OT ไม่คิดจากเช็คเอาท์แล้ว)**.
- lead_tech/tech ขอ OT ได้ (attendance เปิดทุกคนอยู่แล้ว). ⚠️ dLoan ไม่มี carry cap เหมือน advance (installment เล็ก—ยอมรับได้).
- **ขอ OT จากใบงาน (v527, 2026-08-02 — needs mig `185_hr_ot_job.sql`).** `hr_ot` เพิ่มคอลัมน์ `job_no text references job_orders(job_no) on delete set null`. **MyJobs.jsx**: การ์ดงานสถานะ `in_progress` ("กำลังทำงาน") มีปุ่ม "⏱️ ขอทำ OT งานนี้" → `OtQuick` → `submitOt({...,job_no})`. `submitOt` รับ `job_no` + pre-185 fallback (regex /job_no/ → ลบ field retry). แสดง job_no ใน: ใบขอ OT ของฉัน (Attendance) + OtTab (HR) + notify title. listMyOt/listOt ใช้ select("*") → job_no ไหลมาเอง.
- **OT ขอแค่เวลาเริ่ม + เช็คเอาท์ตอนทำเสร็จ (v528, 2026-08-02 — needs mig `186_hr_ot_checkout.sql`).** เปลี่ยน flow: ขอ OT = ระบุ **วัน + เวลาเริ่มเท่านั้น** (ไม่มีเวลาเลิก, hours=0 ตอนขอ) → HR อนุมัติ → พนักงานกด **"เช็คเอาท์ OT"** ตอนทำเสร็จ (default เวลาเลิก=ตอนนี้) → `checkoutOt(id,time_to)` set time_to+hours (`otHoursFromTimes`).
  - **mig 186:** RLS policy `hr_ot_self_checkout` — self UPDATE ได้เฉพาะแถวตัวเอง status='approved' (with check ยัง approved). ⚠️ RLS จำกัดคอลัมน์ไม่ได้ — พนักงานแก้ hours เองได้ในทางเทคนิค (ยอมรับได้ทีมเล็ก, เหมือน submit เดิม).
  - **api:** `submitOt` hours=0 ถ้าไม่มี time_to · เพิ่ม `checkoutOt(id,time_to)` (verify approved + hours>0 + notify HR).
  - **นับเงินเฉพาะที่เช็คเอาท์แล้ว:** PayrollTab filter `status==='approved' && hours>0` (line ~1008) · Attendance openMyPay filter `approved && hours>0`. **อนุมัติแต่ยังไม่เช็คเอาท์ = hours 0 = ไม่คิดเงิน + ไม่ถูก markOtPaid → ค้างไว้จนเช็คเอาท์** (ไม่หายไปที่ 0).
  - **UI:** Attendance `OtForm` เอา field เวลาเลิกออก · `OtCheckout` (ปุ่ม 🏁 เช็คเอาท์ + time input default now) โผล่เมื่อ approved&&!hours · badge "อนุมัติ · รอเช็คเอาท์" · MyJobs `OtQuick` เอาเวลาเลิกออก · HR OtTab แสดง "รอเช็คเอาท์".
- **เตือนลืมเช็คเอาท์ + HR เช็คเอาท์แทน (v529, 2026-08-02 — ไม่มี migration ใหม่).** (1) Attendance: แถบส้มเตือนเมื่อมี OT `approved && !hours && ot_date < today` (บอกจำนวน+ล่าสุด). (2) `hrCheckoutOt(id,time_to)` (api) — HR เช็คเอาท์แทน (ใช้สิทธิ์ผู้จัดการ RLS mig 184 update, ไม่จำกัด user_id, notify พนักงาน) · HR OtTab ปุ่ม "🏁 เช็คเอาท์แทน" (`HrOtCheckout` component) โผล่เมื่อ awaitCheckout.
- **ลา: คงเหลือตาม HR + บล็อกยื่นเกินสิทธิ์ (v554, 2026-08-02 — ไม่มี migration).** Attendance คิด `remainByType` = quota(myQuota override ทับ baseQuota) − usedByType − pendByType (unpaid ไม่จำกัด) ส่งเข้า LeaveForm · LeaveForm: option ประเภทโชว์ "(เหลือ N วัน)"/"เต็มแล้ว" + **disable ประเภทที่เต็ม** (ยกเว้น unpaid) · submit บล็อกถ้า noQuota/overLimit (days>avail) + ปุ่มส่ง disabled + เตือนแดง · unpaid = ทางออกเมื่อสิทธิ์เต็ม. **หมายเหตุ:** การ์ด "วันลาคงเหลือ" ยังโชว์ quota−used (ไม่ลบ pending) ส่วนฟอร์มใช้ quota−used−pending (เข้มกว่า เพื่อบล็อก).
- **KPI ตามตำแหน่งในรายงานประสิทธิผล (v534, 2026-08-02 — ไม่มี migration ใหม่).** PerfTab (HR.jsx) import `ROLE_GUIDE, DEPT_COLOR` จาก handbook · คลิกชื่อพนักงาน → `KpiDetailModal` แสดง KPI ของ role นั้น (m/t/f/src/w จากคู่มือ) + **เติมค่าจริงที่วัดได้** ผ่าน `kpiActual(k,r)` (heuristic จับคำใน m+src): ตรงเวลา→onTime%, มาทำงาน→present ratio, เคลม→m.claims, คะแนน/ดาว→avgRating, งานเสร็จ→m.done(ทีม), เลื่อนนัด→m.resched, OT→otHours · จับไม่ได้ = "ประเมินเอง ดูที่ [src]". `profiles.role` แมพตรง `ROLE_GUIDE` key ทุก role. **ค่าจริงบางตัวเป็นรายทีม (done/rating/claims จาก jm[team]) ไม่ใช่รายคน** — บอกในโมดัลว่า "งานของทีม". KPI ส่วนใหญ่ (ยอดขาย/DSO/checklist) วัดอัตโนมัติไม่ได้ → โชว์เป้า+เมนูที่ต้องไปดู. ดู [[permissions-system]] handbook.
- **กันเวลา OT ทับงาน + ปุ่มทำ OT เสร็จใต้เมนูเวลา (v533, 2026-08-02 — ไม่มี migration ใหม่).** (1) **ตัวกันเวลา OT ทับเวลางานปกติ** (client-side, Attendance OtForm): วันทำงาน (`isWorkday(date,pattern,satGroup,holidays)`) เวลาเริ่ม OT ต้องไม่ก่อน `settings.end` (เวลาเลิกงาน) — ทับ→ขอบ input แดง + เตือนแดง + ปิดปุ่มส่ง; วันหยุดทำได้ทั้งวัน (ไม่เช็ค). OtForm รับ prop settings/pattern/satGroup/holidays เพิ่ม. **ไม่มี guard ฝั่ง server** (เป็น HR tool ภายใน, HR แก้ผ่าน hrEditOt ได้). (2) **ปุ่ม "🏁 ทำ OT เสร็จ (เช็คเอาท์)" ปุ่มใหญ่ ใต้การ์ดเวลาเข้า-ออกปกติ** (Attendance): โผล่เมื่อมีใบ OT `ot_date===today && approved && hours=0`; ใช้ `<OtCheckout big>` (เพิ่ม prop `big` → att-big). เดิมปุ่มเช็คเอาท์อยู่แค่ในการ์ด "ใบขอ OT ของฉัน" ด้านล่าง — ย้ายมาโชว์ด้านบนด้วยให้กดง่าย. เปลี่ยน label ปุ่มเป็น "ทำ OT เสร็จ (เช็คเอาท์)". OT ยังแยก attendance (คนละตาราง) ดู [[i18n-burmese]] สำหรับพม่า.
- **รีวิว+แก้หน้าประสิทธิผล/รายงาน (v531, 2026-08-02 — ไม่มี migration ใหม่).** ผลรีวิว KPI: สูตรคะแนน (ตรงเวลา50/มา20/คะแนนงาน30 − เคลม×5, normalize ด้วย wsum) ถูก · rating/is_claim อยู่บน job_orders จริง (mig 042, ตั้งค่าผ่าน saveJobReview: โมดัลอนุมัติใน JobOrders + Subcontractor review) · วันลาเต็มวันไม่นับ workday (ถูก). **แก้ 4 จุด:** (1) **OT ในรายงาน:** ReportTab+PerfTab+CSV เดิมคิด OT จาก `dayStat` (auto เช็คเอาท์) → เปลี่ยนเป็นดึงจาก `listOt()` sum ต่อคน (`ot_date∈[from,calcTo]` && status∈approved/paid && hours>0) เก็บ `otByUser` (ตรงกับเงินเดือน). (2) **ผูกทีมผ่านรอบนัด:** PerfTab `jm` เดิม `if(!j.assigned_team)return` → ใช้ team = `j.assigned_team || visits.map(v=>v.assigned_team).find(Boolean)`, วันงาน = job.scheduled_at + ทุก visit (นับถ้ามีวันตกในเดือน) กันงานที่มอบผ่าน job_visits หลุด. (3) **ตรงเวลาไม่ปนขาด:** onTime = `present ? (present−lateCnt)/present : null` (เดิม /workdays ทำให้การขาดหักซ้ำกับหมวดมาทำงาน). (4) แก้ sec-sub/footnote ให้ชัดว่า OT/เลื่อนนัด = ข้อมูลประกอบ ไม่คิดคะแนน · ReportTab detail รายวัน tag "OT" → "เกินเวลา" (ไม่ใช่ OT จ่ายจริง).
- **HR แก้เวลา OT + เลขงาน/รายละเอียดในสลิป (v530, 2026-08-02 — ไม่มี migration ใหม่).** (1) `hrEditOt(id,{time_from,time_to})` (api) — HR แก้เวลาเริ่ม/เลิก OT, คิดชั่วโมงใหม่, `.neq('status','paid')` · HR OtTab ปุ่ม "✏️ แก้เวลา" (`HrOtEdit`) โผล่เมื่อ status≠paid. (2) `listOt` enrich `jobCustomer`/`jobDetails` ผ่าน `_jobBriefMap(jobNos)` (query job_orders+customers, .catch→{}); OtTab โชว์ "🔧 งาน JOB-xxx · ลูกค้า · รายละเอียด". (3) **PayDetail ส่วน OT ดึงจากใบขอจริง**: prop `otRows` — ถ้ามีใช้มัน (โชว์ วันที่/เวลา/เลขงาน+รายละเอียด/ชม.), ถ้าไม่มี fallback `otDays` (auto เก่า). PayrollTab: load `listOt()` (ทั้งหมด) → `otRowsByUser` (approved+hours>0 หรือ paid&period===ym ในรอบ) ส่งเข้า PayDetail · Attendance openMyPay ส่ง `otRows: otIn`. **แก้บั๊ก:** เดิม PayDetail โชว์ OT อัตโนมัติจาก attendance (ไม่ตรงหัวข้อที่ override จากใบขอ).

**Pay detail drill-down + employee self-view (v444–v445, 2026-07-17 — needs mig `149_payslip_self_read.sql`).** `periodStats` now also returns `days[]` (per-day records: work {lateMin, otH, otCounted, leaveH} / absent / leave {lt, frac} / holiday {hours, normH, otH, noOut}) — single source for detail views. `PayDetail.jsx` = shared `PayDetailModal` used by (1) HR PayrollTab: click ANY cell in a row → per-day breakdown of every column (OT per day + uncertified flag, late minutes per day, absent dates, holiday pay per day with formula, leaves + over-quota explanation, SSO base, advance rows with reasons); (2) Attendance.jsx card "🧾 เงินเดือนของฉัน (ละเอียด)": employee picks a month → `openMyPay()` recomputes from their OWN rows (listMyAttendance/listMyLeaves/listMyAdvances — RLS self-scoped) with the SAME periodStats+computePayslip+over-quota formula as HR; slip (bonus/other_deduct/d_advance/status/net) read via listPayslips(ym) once mig 149 (payslips self-SELECT policy) is run — without it the card still works, computed live, note says round not yet booked. Advance rows shown: `period === ym` (settled that round) or approved-unsettled.

**Holiday pay auto (v443, 2026-07-17 — needs migration `148_payslip_holiday.sql`, owner: "จ่ายตามกฎหมายเลย").** ค่าทำงานวันหยุดคิดอัตโนมัติแล้ว (ไม่ใช่ manual ผ่านโบนัสอีกต่อไป — ยกเลิกข้อความใน v425 ข้อ "Holiday work surfaced"). `periodStats` splits holiday attendance per day: work minutes = span − 1h lunch when span > 5h (mirrors 08–17 = 8h), first ≤8h → `holNormHours`, beyond → `holOtHours` (½-hr blocks via otCreditHours). `computePayslip`: `holPay = holNorm×hourly×(monthly?1:2) + holOt×hourly×3` (พ.ร.บ. ม.62–63: รายเดือนได้เพิ่ม 1 เท่า, รายวัน 2 เท่า, OT วันหยุด 3 เท่า) → included in gross/net. Payslips carry `hol_pay` (mig 148; savePayslip/s strip on pre-148 error). UI: new "ค่าวันหยุด" column (hover = breakdown), slip print + DM slip line, chip title updated. วันหยุดนักขัตฤกษ์นับด้วยอยู่แล้วผ่าน holSet (hr_holidays) ใน isWorkday.

**Position = role, single source of truth (v215, 2026-06-28).** HR job position ("ตำแหน่งงาน") is no longer a separate free-text `profiles.department` + editable positions list — it's now **derived from the person's `role`** via `ROLE_LABEL` (from [[permissions-system]]) at the data layer. `posLabel(p) = ROLE_LABEL[p.role] || p.department` lives in api.js and is applied in `listHrStaff` / `listAttendance` / `listLeaves` / `listAdvances`, so every HR tab shows the same position as ตั้งค่า. The StaffTab editor (add/remove positions + per-staff department input + `getPositions`/`savePositions`/`DEFAULT_POSITIONS` + `hr-positions` datalist) is gone, replaced by a **read-only** reference card listing the role-based positions held by **permanent staff** (counts per position). To change someone's position, assign their role in ตั้งค่า → ผู้ใช้งาน. `listHrStaff` already returns permanent staff only (excludes `teams.type='sub'` members). Also fixed a latent bug: `setPattern` was `(p, fields)` but the work_pattern/sat_group `<select>`s call it `(p, key, val)` — it now accepts both, so those dropdowns actually save.
