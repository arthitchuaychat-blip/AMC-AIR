---
name: handover-system
description: "ใบส่งมอบงาน — technician-filled service handover form (multi-form builder + on-screen signatures), prints/PDFs the saved data."
metadata: 
  node_type: memory
  type: project
  originSessionId: b8d73488-693d-46d1-b5f3-7849cf1f6166
---

ใบส่งมอบงาน (job handover) is a digital form the **technician fills while on the job**, optionally linked to a job order (or standalone). Migration **077** (`job_handovers` table); run manually in Supabase.

- Structure: one handover = header (customer + ประเภทงาน + 2 signatures) + a list of **sub-forms** in a `forms` jsonb. **3 main types (v343, ADD_KINDS)** offered by "+ เพิ่มแบบฟอร์ม": `accept` = ส่งมอบงานติดตั้ง (multi-unit in ONE form: machines[], rows[13 items][machine]='pass'|'fail'|null in 5 ACCEPT_GROUPS, itemNotes, overall[4], photos[] unlimited; customer signature relabeled ผู้ตรวจสอบ/ผู้รับมอบงาน) · `clean` = ส่งมอบงานล้าง per machine (acts=PM 15 done/not + rows=CLEAN_ROWS 16 b/a + photosBefore/photosAfter ≤4 each) · `repair` = ส่งมอบงานซ่อม per machine (rows=REPAIR_ROWS 13 b/a + fix text + 4+4 photos). Legacy kinds `perf`/`pm` hidden from the add menu but old docs still open/print. Photos upload via uploadMaterialPhoto (PhotoPicker in HandoverEditor).
- Shared defs live in `lib/handover.js` (PERF_ROWS, PM_ROWS, WORK_TYPES, blank factories) — used by BOTH the editor and the print renderer so they stay in lockstep.
- Components: `HandoverEditor.jsx` (the fill-in form), `SignaturePad.jsx` (draw-on-screen tech+customer signatures → PNG via `uploadSignatureDataUrl`), `JobHandover.jsx` (print/PDF renderer of SAVED data, paginates across A4), `Handover.jsx` (the module: list/create/edit/print/delete).
- Entry points: หน้า **"งานของฉัน"** on in-progress jobs + **ใบงาน** detail popup → `onHandover(jo)` opens the editor; module "handover" allows standalone create (ไม่ผูกใบงาน). New nav module `handover` in [[permissions-system]] (tech+lead_tech+office = edit; stock = none).
- **Two-phase fill**: tech fills ก่อน (before-work column) → บันทึกร่าง (draft); after work, taps the same job button → it **reopens that job's existing draft** (Handover.jsx startJob effect finds `status==='draft'`) to fill หลัง + checklist → บันทึก & ส่ง (submitted). The job button only continues a draft; once submitted it starts fresh.
- RLS: ช่างจัดการของตัวเอง (`created_by = auth.uid()`); admin/exec/sales/finance/lead_tech see all.
- Print uses native browser pagination (no `.doc-sheet`), so the printDoc paginator no-ops — see [[print-pagination]]. Verify print/editor layout in the `_design/` harness (handover.html / handover-editor.html, not committed).
- Earlier draft (v198) was a blank printout from the job; superseded by this tech-filled version (v199).

**Field-zone hardening batch (v446, 2026-07-17 — needs migration `150_field_status_guards.sql`).** Full review of MyJobs/JobOrders/Handover/JobTimeline; mig 150 rewrites the status RPCs + handover RLS:
- `set_visit_status(p_visit_id, p_status, p_job_override=null, p_lock=null)` (old 2-arg dropped; defaults keep old callers working): guards — job cancelled → raise; tech: locked job / done visit → raise, status limited to in_progress/awaiting_approval/reschedule, no override/lock. Override+lock happen in the SAME transaction (อนุมัติ "เสร็จ→รอทำใบเสนอราคา/ล็อก" เดิมเป็น 3 คำสั่งแยก เน็ตสะดุดค้างผิดคิว). `recompute_job_status` returns early if job cancelled (kills resurrection). New `set_job_status(p_job,p_status)`: office/lead_tech any; tech = field transitions only on own-team non-locked jobs — fixes the silent-no-op where role tech's direct job_orders UPDATE hit 0 rows via RLS but UI said success (api updateJobStatus/setJobStatus now use this RPC w/ pre-150 fallback that checks row count). setJobStatus('cancelled') → RPC also cancels unfinished visits.
- `saveJobOrder`: blocks saving over a cancelled job; visit replace now (a) matches editor rows to DB by visit `id` and uses FRESH DB statuses (office form open while tech submits no longer clobbers), (b) checks delete error, (c) restores backed-up rows if insert fails (visits no longer wiped on flaky net), (d) computes head status from merged rows and preserves `quote_pending` when all visits done (เดิมแก้ typo แล้วงานหลุดคิวทำใบเสนอ). JobOrders visitFromRow/save carry `id`; viewing-modal approve buttons gated `jo.status !== "cancelled"`.
- Handover: submit validates (confirm listing missing forms/signatures); delete requires reason + logAudit snapshot; editing a submitted sheet asks confirm; RLS split (mig 150) — creator writes drafts only, submitted = office roles, delete = admin or own draft; `saveHandover(submitted)` posts job_logs "📝 ส่งใบส่งมอบงานแล้ว" + notifies watchers; MyJobs shows the handover button for all non-readonly statuses (was in_progress only — techs couldn't fill ก่อน/collect signature after ส่งอนุมัติ).
- 1000-row caps fixed: `listJobBriefs`, `_jobAggregate`'s `jobs` table (closed jobs "reopened" past 1k), `listHandovers` → `_fetchAll`.
- MyJobs: 🔄 refresh chip + reload on visibilitychange; busy-guard on status buttons; "ขอนัดหมายเพิ่ม" requires a reason (posted to timeline) and can be undone ("กลับไปทำต่อ") at visit+job level; renders `confirmItems` (เครื่อง/บริการจากใบเสนอ ไม่มีราคา); `quote_pending` bucket = เสร็จแล้ว tab; JobTimeline labels derive from JOB_STATUSES (+quote_pending action; `_JOB_ST_TH` too).
- DEFERRED (agreed): tech phones still download the whole job book + quote prices via listJobOrders (needs a team-scoped query; touching quotations RLS risks breaking office views) — revisit as its own task.

**Verify round (v447, 2026-07-17) — fixes to v446's own bugs (adversarial + sweep agents, both confirmed):**
- `saveJobOrder` fresh-status merge was UNCONDITIONAL → broke startReschedule (round never closed, job stuck in นัดหมายเพิ่ม queue), unlockVisit, and the per-round status Combo. Now editor rows carry `_orig` (status at load); DB status wins ONLY when `status === _orig` (form untouched) — deliberate edits win.
- Backup-restore of visits NEVER worked: `job_visits.id` is `generated always as identity` → insert-with-id rejected (428C9) and the restore error wasn't checked. Now strips `id` on restore + appends a ⚠️ to the error if restore also fails. Restored rows get new ids (stale phones may see 'visit not found' — acceptable).
- Locked-job trap: "📅 เสร็จ รอนัดหมายเพิ่ม" locks the job; after mig 150 techs hit the locked guard forever. `saveJobOrder` now auto-unlocks when a locked job's merged status becomes pending/scheduled/in_progress (office scheduled a new round); MyJobs hides "กลับไปทำต่อ" when `jo.locked`.
- `TECH_READONLY` += `quote_pending` (techs could still withdraw stock/handover/post on quote_pending jobs; no-visit card showed no label — added 📝 label).
- Job-level (no-visit) reschedule now also requires the reason prompt (was visit-level only); reason-log failure flashes a warning instead of silently losing the reason.
- MyJobs buckets: `myTeamDone(j)` — multi-team job where MY team's visits are all done/cancelled → moves to เสร็จแล้ว tab (was pinned in "ต้องทำ (วันนี้)" forever via jobAt falling back to the done visit's past date).
- Handover: editing a submitted sheet keeps status submitted (single "บันทึก" button — draft demotion would make it tech-editable/deletable again under mig 150 policies); `saveHandover` notifies only on the draft→submitted TRANSITION (was every save); tech's แก้ไข button hidden on own submitted sheets (RLS would 0-row + PGRST116 — also mapped to a Thai error); HandoverEditor: dirty-guard on overlay/✕ + 1s-debounced localStorage snapshot with restore-offer (battery死/camera-reload no longer wipes 20 fields).
- `_jobWatchers` += lead_tech · dead `setJobVisitsStatus` removed (carried the silent-RLS no-visit bug) · `listJobBriefs` customers query failure degrades to no-names instead of rejecting the whole page · saveJobOrder pre-check selects now throw on error · edit job_log records the ACTUAL written headStatus.

**รีวิว+ปรับใบส่งมอบงาน เฟส 1 (v557, 2026-08-02 — ไม่มี migration).** เจ้าของขอรีวิว → เลือกทำ 4 ข้อเสี่ยงต่ำก่อน (ฝั่ง client ล้วน):
- **เติมชื่อช่างอัตโนมัติ:** HandoverEditor รับ prop `me` (จาก App = profile.name||email) → baseline เติม `tech_name=me` ถ้ายังว่าง (ทำใน `base` useMemo ไม่ให้เป็น dirty ทันที · dirty/restore เทียบกับ base แทน initial).
- **จำลายเซ็นช่าง:** หลังบันทึกเก็บ `tech_sign_url` (ที่อัปเป็น URL แล้ว) ลง `localStorage["amc_tech_sign"]` · ปุ่ม "↩ ใช้ลายเซ็นเดิม" ข้างแท่นเซ็นช่าง (โผล่เมื่อมี lastSig && !tech_sign_url) — set เป็น URL ตรง ๆ persist ไม่ re-upload (เช็ค startsWith('data:')).
- **เตือนเช็คลิสต์ว่างก่อนส่ง:** `formEmpty(f)` (ต่อ kind: inst/wash เช็ค checks+meas, fix เช็ค diag/rep/symptoms/meas, pmc/accept/clean/pm/repair/perf ตามหลัก) → รวมในรายการ miss ตอน submit ครั้งแรก (ส่งต่อได้ถ้าตั้งใจ) พร้อมลายเซ็นลูกค้า/ช่าง.
- **ซ่อนราคาอะไหล่บนใบลูกค้า (ฟอร์มซ่อม):** FixCard เพิ่ม checkbox `f.show_price` (เก็บใน forms jsonb ไม่ต้อง migration) · JobHandover พิมพ์คอลัมน์ราคา/รวม+แถวรวมเฉพาะเมื่อ show_price (ปกติซ่อน — ราคาจริงคิดที่ใบแจ้งหนี้).
- **แก้ hint สองเฟส** ให้ตรงจริง (ก่อน/หลังใช้เฉพาะงานล้าง/ซ่อม).
- **(6) งานหลายเครื่อง — ทำแล้ว (v558):** ปุ่ม **⧉ ทำซ้ำฟอร์ม** บนหัวการ์ดทุกฟอร์ม (`dupForm` deep-clone JSON แทรกหลัง i) — กรอกเครื่องแรกเสร็จ ทำซ้ำแล้วแก้จุดต่าง · thread `onDup` ผ่าน FormCard→AcceptCard/NewFormShell (Inst/Wash/Fix/Pmc). เลือกวิธีนี้แทนรื้อ inst เป็นหลายเครื่อง/เอา accept กลับ (เสี่ยงต่ำ ช่วยทุกฟอร์มรายเครื่อง).
- **(2) ดึงเครื่องจากใบเสนอ — ทำแล้ว (v559):** `parseAcMachine(name)` (handover.js) จับยี่ห้อจาก AC_BRANDS + BTU จาก regex + รุ่นจาก "รุ่น/model X" · HandoverEditor โหลด `getQuoteItems(initial.doc_ref)` กรอง `kind==='ac' || /btu/` → `acUnits` (ขยายตาม qty ≤30) · เมนูเพิ่มฟอร์มมีปุ่ม "🧾 สร้างฟอร์มติดตั้ง/ล้าง ×N" (`makeFromQuote`) สร้าง 1 ฟอร์ม/เครื่อง เติม brand/model/btu + ชื่อเต็มใน note. mapping เป็น best-effort (ชื่อสินค้าเป็น free text).
- **(4) นัดล้าง/PM รอบถัดไป → แจ้งเตือน+ปฏิทิน — ทำแล้ว (v560, needs mig `192_service_reminders.sql`):** เจ้าของเคาะ "แจ้งเตือน+ลงปฏิทิน" (ไม่สร้าง job อัตโนมัติ). ตาราง `service_reminders` (due_date/kind/customer_name/contact_phone/job_no/handover_id/status open|done|dismissed · RLS: read all · insert all (ช่างส่งใบสร้างเอง) · update ผู้จัดการ+ผู้สร้าง · delete ผู้จัดการ). **saveHandover** ตอน submit ครั้งแรก scan forms wash/pmc ที่มี next_date → insert reminders + notify(admin/exec/sales/hr) (best-effort, pre-192 ข้าม). **api:** createServiceReminder/listServiceReminders(status)/setReminderStatus. **calendar.js (ICS):** ดึง service_reminders status=open (เฉพาะฟีดรวมทุกทีม ไม่ใช่ team feed) → all-day VEVENT (DTSTART;VALUE=DATE + DTEND วันถัดไป) Google เตือนวันนั้น. **UI:** Handover.jsx การ์ด "🔔 นัดบริการรอบถัดไป" เหนือ filter (โหลด listServiceReminders('open')) · เกินกำหนดขึ้นแดง+⚠️ · ปุ่ม ✓นัดแล้ว/เสร็จ (done) · ปิด (dismissed).
- **เฟส 2 ที่ยังเหลือ:** (3) ส่งใบให้ลูกค้าทาง LINE (ก้อนใหญ่สุด — กำลังทำ) · (8) ประทับเวลา+พิกัดตอนเซ็น (migration signed_at) · (9) รูปกำพร้าใน storage ตอนยกเลิก · (10) draftKey 'ho-draft-new' ชนกัน.
