# Customer chat drag/drop and clipboard — v837 live

14 Sep 2026: owner requested dragging image/video/file attachments into customer chat and pasting screenshots with Ctrl+V, asking to confirm understanding. Implemented on `work/customer-chat-drop-paste`, based on live main `4629fe8`. Production rollout authorized; PR #7 merged as `01818ade1a854ae82d01e2f79b02f3d8fabaa41a`; all four Vercel deployment checks succeeded. The deployed tree `92605fc30d8bea0f08a435352156f58f83587cb8` exactly matches the tested local tree. Both live app domains returned HTTP 200 serving `/assets/index-DBe58ZiR.js` with v837 and Chat chunk `/assets/Chat-DFjynO6E.js`. Browser loaded the new asset and login screen with no application errors. No actual customer messages sent and no production database writes/migrations.

- Chat.jsx file picker, drag/drop over the thread and textarea paste share the upload queue in lib/chatAttachments.js. Text-only paste stays native; Ctrl/Cmd+V images become pending attachments and require Send. Enter sends; Shift+Enter inserts newline; IME composition does not send.
- Existing image upload compression/storage is reused. File MIME is normalized before upload so extension-only MP4 and arbitrary files do not default to image/jpeg. Limit 25 MB/file; images use existing compression. Video preview appears before send. LINE video/documents remain clickable links; Facebook multipart attachment type now identifies video from content type. No new schema or auth changes.
- Queue generation invalidates uploads on contact/channel/permission changes, including A → B → A. Prevent send while uploading and concurrent upload batches. Successfully sent attachments are removed individually so partial-failure retry does not resend them. Old send completion cannot clear the next room's attachments/text.
- `npm run test:chat-attachments` passes: clipboard deduplication, drag files, MIME/size, upload failure, late upload/room isolation, actual Chat handler execution, send gate, LINE/FB routing, partial-send retry, old-send completion, and mocked Messenger multipart image/video/file endpoint. Test fixtures make no real HTTP calls.
- `npm run build` passes; `node --check api/fb-send.js` passes; chat search 10/10. `npm test` still fails existing Receipts unpaid status badge; undefined-vars still reports pre-existing Settings `m` and api.js loan `status`, no new globals. Full authenticated browser/customer delivery not exercised for this batch; no actual customer test messages.

References checked: MDN paste event https://developer.mozilla.org/en-US/docs/Web/API/Element/paste_event ; Meta asset types/25 MB https://developers.facebook.com/documentation/business-messaging/messenger-platform/send-messages/saving-assets.md ; Supabase standard uploads https://supabase.com/docs/guides/storage/uploads/standard-uploads . Live storage bucket metadata read-only: photos has no bucket-specific MIME or size override.
